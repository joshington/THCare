import React from "react";

const GenderInputSelct = (props) => {
    <select {...props}>
        <option>Select Gender</option>
        <option value="male">Male</option>
        <option value="female">Female</option>
    </select>
}

export default GenderInputSelct